<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="e.rick.duolingoclone">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:name=".DuolingoApp"
        tools:ignore="GoogleAppIndexingWarning">

        <activity android:name=".ui.tasks.WordTask.WordTaskActivity" />

        <activity android:name=".ui.tasks.TranslateSentenceTask.TSTaskActivity"
            android:windowSoftInputMode="adjustPan"/>

        <activity android:name=".ui.tasks.TapPairTask.TapPairActivity" />

        <activity android:name=".ui.activity.LessonCompletedActivity.LessonCompletedActivity" />

        <activity android:name=".ui.activity.WelcomeActivity.WelcomeActivity" />

        <activity android:name=".ui.activity.SignInActivity.SignInActivity" />

        <activity android:name=".ui.activity.SelectLanguageActivity.SelectLanguageActivity" />

        <activity android:name=".ui.activity.PickDailyGoalActivity.PickDailyGoalActivity" />

        <activity android:name=".ui.activity.lessonlistactivity.LessonListActivity">

        <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>

        </activity>

    </application>

</manifest>