<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="e.rick.duolingoclone">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Presentation.Tasks.WordTask.WordTaskActivity" />

        <activity android:name=".Presentation.Tasks.TranslateSentenceTask.TSTaskActivity"
            android:windowSoftInputMode="adjustPan"/>

        <activity android:name=".Presentation.Tasks.TapPairTask.TapPairActivity" />

        <activity android:name=".Presentation.Activity.LessonCompletedActivity.LessonCompletedActivity" />

        <activity android:name=".Presentation.Activity.WelcomeActivity.WelcomeActivity" />

        <activity android:name=".Presentation.Activity.SignInActivity.SignInActivity" />

        <activity android:name=".Presentation.Activity.SelectLanguageActivity.SelectLanguageActivity" />

        <activity android:name=".Presentation.Activity.PickDailyGoalActivity.PickDailyGoalActivity" />

        <activity android:name=".Presentation.Activity.LessonListActivity.LessonListActivity">

        <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>

        </activity>

    </application>

</manifest>